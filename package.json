{
  "name": "validador-de-campos",
  "version": "1.0.0",
  "description": "validadorCampos",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite --open",
    "lint": "eslint -c .eslintrc.json --no-inline-config --no-error-on-unmatched-pattern --ext .js ."

 },
  "author": "Fellipe Barnabé",
  "license": "ISC",
  "dependencies": {
    "@trybe/eslint-config-frontend": "^1.11.4",
    "validator": "^13.11.0",
    "vite": "^4.4.9"
  }
}
